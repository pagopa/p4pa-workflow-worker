server:
  http: "\${SERVER_PORT:8080}"

spring:
  application:
    name: ${artifactId}
    version: ${version}
  jmx.enabled: true
  threads:
    virtual:
      enabled: "\${VIRTUAL_THREADS_ENABLED:true}"
  temporal:
    namespace: "\${TEMPORAL_SERVER_NAMESPACE:pu}"
    connection:
      enable-https: "\${TEMPORAL_SERVER_ENABLE_HTTPS:false}"
      target: "\${TEMPORAL_SERVER_HOST:localhost}:\${TEMPORAL_SERVER_PORT:7233}"

    # Listing workers and registered activities
    workers:

      # generic
      - task-queue: LowPriorityWF
        capacity:
          activity-task-pollers-configuration:
            poller-behavior-autoscaling:
              enabled: true
              initial-concurrent-task-pollers: "\${WF_LOW_PRIORITY_MIN_POLLER_SIZE:1}"
              min-concurrent-task-pollers: "\${WF_LOW_PRIORITY_MIN_POLLER_SIZE:1}"
              max-concurrent-task-pollers: "\${WF_LOW_PRIORITY_MAX_POLLER_SIZE:3}"
        activity-beans:
          - brokersRetrieverActivityImpl
          - organizationBrokeredActiveRetrieverActivityImpl
          - organizationPaymentsReportingPagoPaListRetrieverActivityImpl
          - organizationPaymentsReportingPagoPaRetrieverActivityImpl
          - synchronizeTaxonomyActivityImpl
          - exportFileExpirationHandlerActivityImpl
          - sendEmailActivityImpl
          - deleteSendNotificationFileActivityImpl
          - deleteSendLegalFactFileActivityImpl
      # debt position
      - task-queue: DebtPositionWF
        capacity:
          activity-task-pollers-configuration:
            poller-behavior-autoscaling:
              enabled: true
              initial-concurrent-task-pollers: "\${WF_DP_LOW_PRIORITY_MIN_POLLER_SIZE:1}"
              min-concurrent-task-pollers: "\${WF_DP_LOW_PRIORITY_MIN_POLLER_SIZE:1}"
              max-concurrent-task-pollers: "\${WF_DP_LOW_PRIORITY_MAX_POLLER_SIZE:3}"
        activity-beans:
          - debtPositionExpirationActivityImpl
          - IONotificationDebtPositionActivityImpl
          - massiveIbanUpdateActivityImpl
      - task-queue: DebtPositionSyncWF
        capacity:
          activity-task-pollers-configuration:
            poller-behavior-autoscaling:
              enabled: true
              initial-concurrent-task-pollers: "\${WF_DP_RESERVED_SYNC_MIN_POLLER_SIZE:1}"
              min-concurrent-task-pollers: "\${WF_DP_RESERVED_SYNC_MIN_POLLER_SIZE:1}"
              max-concurrent-task-pollers: "\${WF_DP_RESERVED_SYNC_MAX_POLLER_SIZE:10}"
        activity-beans:
          - synchronizeInstallmentAcaActivityImpl
          - synchronizeInstallmentGpdPreLoadActivityImpl
          - synchronizeInstallmentGpdActivityImpl
          - finalizeDebtPositionSyncStatusActivityImpl
      # debt position custom WF sync
      - task-queue: DebtPositionCustomSyncWF
        capacity:
          activity-task-pollers-configuration:
            poller-behavior-autoscaling:
              enabled: true
              initial-concurrent-task-pollers: "\${WF_DP_RESERVED_CUSTOM_SYNC_MIN_POLLER_SIZE:1}"
              min-concurrent-task-pollers: "\${WF_DP_RESERVED_CUSTOM_SYNC_MIN_POLLER_SIZE:1}"
              max-concurrent-task-pollers: "\${WF_DP_RESERVED_CUSTOM_SYNC_MAX_POLLER_SIZE:5}"
        activity-beans:
          - debtPositionSynchronizeFineActivityImpl
          - debtPositionFineReductionOptionExpirationActivityImpl
      # ingestion flow
      - task-queue: IngestionFlowFileWF
        capacity:
          activity-task-pollers-configuration:
            poller-behavior-autoscaling:
              enabled: true
              initial-concurrent-task-pollers: "\${WF_IMPORT_MEDIUM_PRIORITY_MIN_POLLER_SIZE:1}"
              min-concurrent-task-pollers: "\${WF_IMPORT_MEDIUM_PRIORITY_MIN_POLLER_SIZE:1}"
              max-concurrent-task-pollers: "\${WF_IMPORT_MEDIUM_PRIORITY_MAX_POLLER_SIZE:3}"
        activity-beans:
          - paymentsReportingIngestionFlowFileActivityImpl
          - handlePaymentsReportingDeletionActivityImpl
          - treasuryOpiIngestionActivityImpl
          - treasuryCsvIngestionActivityImpl
          - treasuryCsvCompleteIngestionActivityImpl
          - treasuryXlsIngestionActivityImpl
          - treasuryPosteIngestionActivityImpl
          - receiptIngestionActivityImpl
          - receiptPagopaIngestionActivityImpl
          - organizationIngestionActivityImpl
          - debtPositionTypeIngestionActivityImpl
          - paymentNotificationIngestionActivityImpl
          - installmentIngestionFlowFileActivityImpl
          - ingestionFlowFileProcessingLockerActivityImpl
          - updateIngestionFlowStatusActivityImpl
          - sendEmailIngestionFlowActivityImpl
          - receiptPagopaNotifySilActivityImpl
          - receiptPagopaSendEmailActivityImpl
          - synchronizeIngestedDebtPositionActivityImpl
          - assessmentsIngestionActivityImpl
          - assessmentsRegistryIngestionActivityImpl
          - debtPositionTypeOrgIngestionActivityImpl
          - orgSilServiceIngestionActivityImpl
          - sendNotificationIngestionActivityImpl
          - fetchAndMergeNoticesActivityImpl
          - deleteMassiveNoticesFileActivityImpl
      # export file
      - task-queue: ExportFileWF
        capacity:
          activity-task-pollers-configuration:
            poller-behavior-autoscaling:
              enabled: true
              initial-concurrent-task-pollers: "\${WF_EXPORT_MEDIUM_PRIORITY_MIN_POLLER_SIZE:1}"
              min-concurrent-task-pollers: "\${WF_EXPORT_MEDIUM_PRIORITY_MIN_POLLER_SIZE:1}"
              max-concurrent-task-pollers: "\${WF_EXPORT_MEDIUM_PRIORITY_MAX_POLLER_SIZE:3}"
        activity-beans:
          - updateExportFileStatusActivityImpl
          - exportFileActivityImpl
          - sendEmailExportFileActivityImpl
      # classification
      - task-queue: ClassificationWF
        capacity:
          activity-task-pollers-configuration:
            poller-behavior-autoscaling:
              enabled: true
              initial-concurrent-task-pollers: "\${WF_CLASSIFICATION_MEDIUM_PRIORITY_MIN_POLLER_SIZE:1}"
              min-concurrent-task-pollers: "\${WF_CLASSIFICATION_MEDIUM_PRIORITY_MIN_POLLER_SIZE:1}"
              max-concurrent-task-pollers: "\${WF_CLASSIFICATION_MEDIUM_PRIORITY_MAX_POLLER_SIZE:3}"
        activity-beans:
          - clearClassifyIudActivityImpl
          - clearClassifyTreasuryActivityImpl
          - iudClassificationActivityImpl
          - clearClassifyIufActivityImpl
          - iufClassificationActivityImpl
          - paymentsReportingImplicitReceiptHandlerActivityImpl
          - transferClassificationActivityImpl
      # pagopa send
      - task-queue: SendNotificationProcessWF
        capacity:
          activity-task-pollers-configuration:
            poller-behavior-autoscaling:
              enabled: true
              initial-concurrent-task-pollers: "\${WF_SEND_RESERVED_NOTIFICATION_MIN_POLLER_SIZE:1}"
              min-concurrent-task-pollers: "\${WF_SEND_RESERVED_NOTIFICATION_MIN_POLLER_SIZE:1}"
              max-concurrent-task-pollers: "\${WF_SEND_RESERVED_NOTIFICATION_MAX_POLLER_SIZE:3}"
        activity-beans:
          - preloadSendFileActivityImpl
          - uploadSendFileActivityImpl
          - deliveryNotificationActivityImpl
          - getSendNotificationActivityImpl
      - task-queue: SendNotificationStreamConsumeWF
        capacity:
          activity-task-pollers-configuration:
            poller-behavior-autoscaling:
              enabled: true
              initial-concurrent-task-pollers: "\${WF_SEND_RESERVED_STREAM_MIN_POLLER_SIZE:1}"
              min-concurrent-task-pollers: "\${WF_SEND_RESERVED_STREAM_MIN_POLLER_SIZE:1}"
              max-concurrent-task-pollers: "\${WF_SEND_RESERVED_STREAM_MAX_POLLER_SIZE:5}"
        activity-beans:
          - getSendStreamActivityImpl
          - getSendNotificationEventsFromStreamActivityImpl
          - updateSendNotificationStatusActivityImpl
          - sendNotificationDateRetrieveActivityImpl
          - fetchSendLegalFactActivityImpl
          - updateLastProcessedStreamEventIdActivityImpl
          - validateSendNotificationStatusActivityImpl
          - getSendNotificationByNotificationRequestIdActivityImpl
          - notifySendNotificationStreamEventsActivityImpl
      - task-queue: SendWF
        capacity:
          activity-task-pollers-configuration:
            poller-behavior-autoscaling:
              enabled: true
              initial-concurrent-task-pollers: "\${WF_SEND_MIN_POLLER_SIZE:1}"
              min-concurrent-task-pollers: "\${WF_SEND_MIN_POLLER_SIZE:1}"
              max-concurrent-task-pollers: "\${WF_SEND_MAX_POLLER_SIZE:3}"
        activity-beans:
          - fetchSendCampaignsActivityImpl
          - alignSendCampaignActivityImpl
      # assessments
      - task-queue: AssessmentCreationWF
        capacity:
          activity-task-pollers-configuration:
            poller-behavior-autoscaling:
              enabled: true
              initial-concurrent-task-pollers: "\${WF_ASSESSMENTS_RESERVED_CREATION_MIN_POLLER_SIZE:1}"
              min-concurrent-task-pollers: "\${WF_ASSESSMENTS_RESERVED_CREATION_MIN_POLLER_SIZE:1}"
              max-concurrent-task-pollers: "\${WF_ASSESSMENTS_RESERVED_CREATION_MAX_POLLER_SIZE:5}"
        activity-beans:
          - assessmentsCreationActivityImpl
      - task-queue: AssessmentsWF
        capacity:
          activity-task-pollers-configuration:
            poller-behavior-autoscaling:
              enabled: true
              initial-concurrent-task-pollers: "\${WF_ASSESSMENTS_MIN_POLLER_SIZE:1}"
              min-concurrent-task-pollers: "\${WF_ASSESSMENTS_MIN_POLLER_SIZE:1}"
              max-concurrent-task-pollers: "\${WF_ASSESSMENTS_MAX_POLLER_SIZE:5}"
        activity-beans:
          - assessmentsRegistryCreationActivityImpl
      - task-queue: AssessmentClassificationWF
        capacity:
          activity-task-pollers-configuration:
            poller-behavior-autoscaling:
              enabled: true
              initial-concurrent-task-pollers: "\${WF_ASSESSMENTS_CLASSIFICATION_MIN_POLLER_SIZE:1}"
              min-concurrent-task-pollers: "\${WF_ASSESSMENTS_CLASSIFICATION_MIN_POLLER_SIZE:1}"
              max-concurrent-task-pollers: "\${WF_ASSESSMENTS_CLASSIFICATION_MAX_POLLER_SIZE:5}"
        activity-beans:
          - assessmentsClassificationActivityImpl
management:
  info:
    build.enabled: true
    java.enabled: true
    git.enabled: true
  otlp.metrics.export.enabled: "\${TRACING_EXPORT:false}"
  tracing:
    sampling.probability: "\${TRACING_SAMPLING_PROBABILITY:1.0}"
    baggage.remote-fields:
      - X-user-id
  observations:
    http.server:
        filter.order: -100
  endpoints:
    jmx:
      exposure.include: "*"
    web:
      exposure:
        include: health, info, prometheus, metrics
      base-path: /actuator
  health:
    mail.enabled: false
  endpoint:
    health:
      probes.enabled: true
      logging.slow-indicator-threshold: "\${HEALTH_ACTUATOR_LOGGER_TIMEOUT_DURATION:PT1S}"
      show-details: "\${HEALTH_ACTUATOR_SHOW_DETAILS:when-authorized}"
      group:
        readiness.include: "*"
        liveness.include: livenessState,diskSpace,ping
    prometheus:
      enabled: true
  metrics:
    tags:
      application: ${artifactId}
    export:
      prometheus:
        enabled: true
        step: 1m
        descriptions: true
    enable:
      jvm: true
      process: true
      system: true
      http: true
      logback: true

springdoc:
  api-docs.version: openapi_3_0
  writer-with-default-pretty-printer: true

logging:
  level:
    root: "\${LOG_LEVEL_ROOT:INFO}"
    it.gov.pagopa: "\${LOG_LEVEL_PAGOPA:INFO}"
    org.springframework: "\${LOG_LEVEL_SPRING:INFO}"
    org.springframework.boot.availability: "\${LOG_LEVEL_SPRING_BOOT_AVAILABILITY:DEBUG}"
    it.gov.pagopa.pu.worker.exception.WorkerExceptionHandler: "\${LOGGING_LEVEL_API_REQUEST_EXCEPTION:INFO}"
    org.springdoc.core.utils.SpringDocAnnotationsUtils: "\${LOG_LEVEL_SPRING_DOC:ERROR}"
    PERFORMANCE_LOG: "\${LOG_LEVEL_PERFORMANCE_LOG:INFO}"
    PERFORMANCE_LOG.API_REQUEST: "\${LOG_LEVEL_PERFORMANCE_LOG_API_REQUEST:\${logging.level.PERFORMANCE_LOG}}"
    PERFORMANCE_LOG.REST_INVOKE: "\${LOG_LEVEL_PERFORMANCE_LOG_REST_INVOKE:\${logging.level.PERFORMANCE_LOG}}"

jwt:
  access-token:
    public-key: "\${JWT_TOKEN_PUBLIC_KEY:-----BEGIN PUBLIC KEY-----
                
                IwIDAQAB
                -----END PUBLIC KEY-----}"

temporal:
  timeouts:
    system-info: "\${TEMPORAL_TIMEOUT_SYSTEM_INFO_SECONDS:5}"
    rpc-long-poll: "\${TEMPORAL_TIMEOUT_RPC_LONG_POLL_SECONDS:70}"
    rpc-query: "\${TEMPORAL_TIMEOUT_RPC_QUERY_SECONDS:10}"
    rpc-generic: "\${TEMPORAL_TIMEOUT_RPC_GENERIC_SECONDS:10}"
